diff --git build/run-in-docker.sh build/run-in-docker.sh
index 763e55e7c..96bb0e80f 100755
--- build/run-in-docker.sh
+++ build/run-in-docker.sh
@@ -79,9 +79,9 @@ if [[ "$DOCKER_IN_DOCKER_ENABLED" == "true" ]]; then
   echo "..reached DIND check TRUE block, inside run-in-docker.sh"
   echo "FLAGS=$FLAGS"
   #go env
-  go install -mod=mod github.com/onsi/ginkgo/v2/ginkgo@v2.23.4
-  find / -type f -name ginkgo 2>/dev/null
-  which ginkgo
+  #go install -mod=mod github.com/onsi/ginkgo/v2/ginkgo@v2.23.4
+  #find / -type f -name ginkgo 2>/dev/null
+  #which ginkgo
   /bin/bash -c "${FLAGS}"
 else
   echo "Reached DIND check ELSE block, inside run-in-docker.sh"
